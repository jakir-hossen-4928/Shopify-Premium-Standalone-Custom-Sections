{% comment %}
  ============================================================
  PRO IMAGE & TRUST TEXT SECTION
  Fully Dynamic - Inherits Theme Fonts & Colors
  ============================================================
{% endcomment %}

{%- liquid
  assign section_id = section.id
-%}

<style>
  #ImageTrustSection-{{ section_id }} {
    /* Section Padding */
    --padding-top-desktop: {{ section.settings.padding_top_desktop }}px;
    --padding-bottom-desktop: {{ section.settings.padding_bottom_desktop }}px;
    --padding-top-mobile: {{ section.settings.padding_top_mobile }}px;
    --padding-bottom-mobile: {{ section.settings.padding_bottom_mobile }}px;
    --max-width: {{ section.settings.max_width }}px;

    /* Typography - inherits from theme */
    --title-size-desk: {{ section.settings.title_size_desk }}px;
    --title-size-mob: {{ section.settings.title_size_mob }}px;
    --title-lh: {{ section.settings.title_lh | divided_by: 10.0 }};
    --title-ls: {{ section.settings.title_ls | divided_by: 100.0 }}em;
    --title-transform: {{ section.settings.title_transform }};

    --subtitle-size-desk: {{ section.settings.subtitle_size_desk }}px;
    --subtitle-size-mob: {{ section.settings.subtitle_size_mob }}px;
    --subtitle-ls: {{ section.settings.subtitle_ls | divided_by: 100.0 }}em;
    --subtitle-transform: {{ section.settings.subtitle_transform }};

    --text-size-desk: {{ section.settings.text_size_desk }}px;
    --text-size-mob: {{ section.settings.text_size_mob }}px;
    --text-lh: {{ section.settings.text_lh | divided_by: 10.0 }};

    --handwriting-size-desk: {{ section.settings.handwriting_size_desk }}px;
    --handwriting-size-mob: {{ section.settings.handwriting_size_mob }}px;

    /* Colors */
    --bg-color: {{ section.settings.bg_color }};
    --title-color: {{ section.settings.title_color }};
    --text-color: {{ section.settings.text_color }};
    --highlight-color: {{ section.settings.highlight_color }};
    --handwriting-color: {{ section.settings.handwriting_color }};
    
    --btn-bg: {{ section.settings.btn_bg }};
    --btn-text-color: {{ section.settings.btn_text_color }};
    --btn-radius: {{ section.settings.btn_radius }}px;
    --btn-padding: {{ section.settings.btn_padding }}px {{ section.settings.btn_padding_x }}px;
    
    --feature-bg: {{ section.settings.feature_bg }};
    --feature-text-color: {{ section.settings.feature_text_color }};
    --feature-border: {{ section.settings.feature_border }};
    --feature-radius: {{ section.settings.feature_radius }}px;
    
    --card-bg: {{ section.settings.card_bg }};
    --card-radius: {{ section.settings.card_radius }}px;

    /* Content Order */
    --mobile-content-order: {{ section.settings.mobile_content_order }};
    --desktop-content-order: {{ section.settings.desktop_content_order }};
  }

  #ImageTrustSection-{{ section_id }} {
    background-color: var(--bg-color);
    padding-top: var(--padding-top-desktop);
    padding-bottom: var(--padding-bottom-desktop);
    overflow: hidden;
  }

  #ImageTrustSection-{{ section_id }} * { box-sizing: border-box; }

  .itt-container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 20px;
    display: grid;
    grid-template-columns: 1fr 1.1fr;
    gap: {{ section.settings.column_gap }}px;
    align-items: center;
  }

  /* Animations */
  .itt-animate {
    opacity: 0;
    transition: opacity 1.2s cubic-bezier(0.25, 0.8, 0.25, 1), transform 1.2s cubic-bezier(0.25, 0.8, 0.25, 1);
  }
  
  .itt-animate.slide-left { transform: translateX(-80px); }
  .itt-animate.slide-right { transform: translateX(80px); }
  .itt-animate.itt-visible { opacity: 1; transform: translateX(0); }

  /* Left Column */
  .itt-content {
    display: flex;
    flex-direction: column;
    gap: 20px;
    text-align: {{ section.settings.text_align }};
    align-items: {{ section.settings.text_align | replace: 'left', 'flex-start' | replace: 'right', 'flex-end' | replace: 'center', 'center' }};
    order: var(--desktop-content-order);
  }

  .itt-subtitle {
    font-family: var(--font-body-family);
    font-size: var(--subtitle-size-desk);
    color: var(--highlight-color);
    font-weight: 600;
    text-transform: var(--subtitle-transform);
    letter-spacing: var(--subtitle-ls);
    margin: 0;
  }

  .itt-title {
    font-family: var(--font-heading-family);
    font-size: var(--title-size-desk);
    color: var(--title-color);
    line-height: var(--title-lh);
    letter-spacing: var(--title-ls);
    text-transform: var(--title-transform);
    font-weight: var(--font-heading-weight);
    margin: 0;
  }

  .itt-text {
    font-family: var(--font-body-family);
    font-size: var(--text-size-desk);
    font-weight: var(--font-body-weight);
    color: var(--text-color);
    line-height: var(--text-lh);
    margin: 0;
  }

  .itt-text p { margin: 0 0 10px 0; }
  .itt-text p:last-child { margin: 0; }

  /* Features Grid - Responsive layout */
  .itt-features {
    display: grid;
    grid-template-columns: repeat(2, auto);
    gap: 12px;
    margin-top: 10px;
    justify-content: {{ section.settings.text_align | replace: 'left', 'flex-start' | replace: 'right', 'flex-end' | replace: 'center', 'center' }};
  }

  /* Odd count: last item centered below */
  .itt-features:has(.itt-feature-tag:last-child:nth-child(odd)) {
    justify-items: center;
  }
  .itt-features:has(.itt-feature-tag:last-child:nth-child(odd)) .itt-feature-tag:last-child {
    grid-column: span 2;
    justify-self: center;
  }

  .itt-feature-tag {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--feature-bg);
    border: 1px solid var(--feature-border);
    padding: 10px 18px;
    border-radius: var(--feature-radius);
    font-size: 13px;
    font-weight: 500;
    color: var(--feature-text-color);
    white-space: nowrap;
  }

  .itt-feature-icon {
    width: 18px;
    height: 18px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .itt-feature-icon img,
  .itt-feature-icon svg {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .itt-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    background: var(--btn-bg);
    color: var(--btn-text-color);
    padding: var(--btn-padding);
    border-radius: var(--btn-radius);
    text-decoration: none;
    font-weight: 600;
    font-size: 14px;
    text-transform: uppercase;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    width: {{ section.settings.btn_full_width | default: 'auto' }};
  }

  .itt-btn:hover { transform: translateY(-2px); box-shadow: 0 10px 20px rgba(0,0,0,0.15); }
  .itt-btn svg { width: 14px; height: 14px; }

  /* Right Column */
  .itt-media-card {
    background: var(--card-bg);
    border-radius: var(--card-radius);
    box-shadow: 0 20px 40px rgba(0,0,0,0.08);
    display: flex;
    flex-direction: column;
    position: relative;
    order: 1;
  }

  .itt-image-wrap {
    position: relative;
    width: 100%;
    padding-bottom: 60%;
    border-radius: var(--card-radius) var(--card-radius) 0 0;
    overflow: hidden;
  }

  .itt-image-wrap img, .itt-image-wrap video {
    position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover;
  }

  .itt-image-overlay {
    position: absolute; inset: 0;
    background: {{ section.settings.overlay_color }};
    opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};
    pointer-events: none;
  }

  .itt-handwriting-wrap {
    position: absolute;
    top: {{ section.settings.hand_pos_v }}%;
    left: {{ section.settings.hand_pos_h }}%;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    transform: rotate({{ section.settings.hand_rotation }}deg);
  }

  .itt-handwriting {
    font-family: {{ section.settings.handwriting_font.family }}, {{ section.settings.handwriting_font.fallback_families }};
    font-size: var(--handwriting-size-desk);
    font-weight: {{ section.settings.handwriting_weight }};
    color: var(--handwriting-color);
    line-height: 1.2;
    text-shadow: {{ section.settings.handwriting_shadow }};
    white-space: pre-line;
    text-align: center;
  }

  .itt-arrow-svg {
    width: {{ section.settings.arrow_size }}px;
    height: auto;
    margin-top: 5px;
    stroke: var(--handwriting-color);
    filter: drop-shadow({{ section.settings.handwriting_shadow }});
    display: {{ section.settings.show_arrow | liquid_if_true: 'block', 'none' }};
  }

  .itt-play-btn {
    position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
    width: 70px; height: 70px;
    background: #fff; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    z-index: 2; cursor: pointer;
    box-shadow: 0 0 0 8px rgba(255,255,255,0.2), 0 10px 20px rgba(0,0,0,0.2);
    transition: transform 0.3s ease;
  }

  .itt-play-btn:hover { transform: translate(-50%, -50%) scale(1.05); }
  .itt-play-btn svg { width: 24px; height: 24px; fill: #000; margin-left: 4px; }

  /* Trust Bar */
  .itt-trust-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 20px 24px;
    background: var(--card-bg);
    border-radius: 0 0 var(--card-radius) var(--card-radius);
    flex-wrap: wrap;
    gap: 16px;
  }

  .itt-trust-left, .itt-trust-right { display: flex; align-items: center; gap: 12px; }
  .itt-avatars { display: flex; align-items: center; }
  .itt-avatar {
    width: 36px; height: 36px; border-radius: 50%;
    border: 2px solid var(--card-bg); object-fit: cover;
    margin-left: -12px; background-color: #eee;
  }
  .itt-avatar:first-child { margin-left: 0; }

  .itt-trust-text { font-size: 13px; font-weight: 600; color: var(--title-color); line-height: 1.3; }
  .itt-trust-text span { display: block; font-weight: 400; color: var(--text-color); font-size: 12px; }

  .itt-rating-box {
    border: 1px solid var(--feature-border);
    padding: 4px 10px; border-radius: 20px;
    font-weight: 700; font-size: 16px; color: var(--title-color);
  }

  .itt-stars-wrap { display: flex; flex-direction: column; gap: 2px; }
  .itt-stars { display: flex; gap: 2px; color: var(--highlight-color); }
  .itt-stars svg { width: 14px; height: 14px; fill: currentColor; }
  .itt-reviews-count { font-size: 11px; color: var(--highlight-color); font-weight: 500; }

  /* Modal */
  .itt-video-modal {
    position: fixed; inset: 0; z-index: 99999;
    display: flex; align-items: center; justify-content: center;
    opacity: 0; pointer-events: none; transition: opacity 0.4s ease;
  }
  .itt-video-modal.active { opacity: 1; pointer-events: auto; }
  .itt-modal-overlay { position: absolute; inset: 0; background: rgba(0,0,0,0.85); backdrop-filter: blur(5px); }
  .itt-modal-content { position: relative; width: 90%; max-width: 1000px; z-index: 2; transform: scale(0.95); transition: transform 0.4s ease; }
  .itt-video-modal.active .itt-modal-content { transform: scale(1); }
  .itt-video-modal video { width: 100%; border-radius: 12px; }
  .itt-close-btn { position: absolute; top: -45px; right: 0; color: #fff; background: none; border: none; font-size: 32px; cursor: pointer; }

  /* Mobile */
  @media screen and (max-width: 990px) {
    #ImageTrustSection-{{ section_id }} { padding-top: var(--padding-top-mobile); padding-bottom: var(--padding-bottom-mobile); }
    .itt-container { grid-template-columns: 1fr; gap: 40px; }
    .itt-animate.slide-left, .itt-animate.slide-right { transform: translateY(40px); }
    .itt-animate.itt-visible { transform: translateY(0); }
    .itt-title { font-size: var(--title-size-mob); }
    .itt-subtitle { font-size: var(--subtitle-size-mob); }
    .itt-text { font-size: var(--text-size-mob); }
    .itt-handwriting { font-size: var(--handwriting-size-mob); }
    
    /* Mobile content order control */
    .itt-content { order: var(--mobile-content-order); }
    .itt-media-card { order: 1; }
    
    .itt-content { align-items: center; text-align: center; }
    .itt-btn { width: 100%; justify-content: center; }
    .itt-image-wrap { padding-bottom: 75%; }
    .itt-trust-bar { flex-direction: column; text-align: center; padding: 20px; }
    .itt-trust-left { flex-direction: column; }
    .itt-trust-right { justify-content: center; }
    
    /* Feature tags responsive */
    .itt-features {
      grid-template-columns: repeat(2, auto);
      justify-content: center;
    }
    .itt-feature-tag { white-space: normal; text-align: center; }
  }
</style>

<section id="ImageTrustSection-{{ section_id }}">
  <div class="itt-container">
    
    <div class="itt-content itt-animate slide-left">
      {% if section.settings.subtitle != blank %}
        <h4 class="itt-subtitle">{{ section.settings.subtitle }}</h4>
      {% endif %}

      {% if section.settings.title != blank %}
        <h2 class="itt-title">{{ section.settings.title }}</h2>
      {% endif %}

      {% if section.settings.text != blank %}
        <div class="itt-text">{{ section.settings.text }}</div>
      {% endif %}

      {% if section.blocks.size > 0 %}
        <div class="itt-features">
          {% for block in section.blocks %}
            <div class="itt-feature-tag" {{ block.shopify_attributes }}>
              <span class="itt-feature-icon">
                {% if block.settings.icon_type == 'image' and block.settings.icon_image != blank %}
                  {{ block.settings.icon_image | image_url: width: 40 | image_tag: loading: 'lazy' }}
                {% elsif block.settings.icon_type == 'svg' and block.settings.svg_code != blank %}
                  {{ block.settings.svg_code }}
                {% else %}
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                    <path d="M9 12l2 2 4-4"></path>
                  </svg>
                {% endif %}
              </span>
              {{ block.settings.title }}
            </div>
          {% endfor %}
        </div>
      {% endif %}

      {% if section.settings.btn_text != blank %}
        <a href="{{ section.settings.btn_link }}" class="itt-btn">
          {{ section.settings.btn_text }}
          <svg viewBox="0 0 24 24"><path d="M9 5l7 7-7 7" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" fill="none"/></svg>
        </a>
      {% endif %}
    </div>

    <div class="itt-media-card itt-animate slide-right">
      <div class="itt-image-wrap">
        {% if section.settings.image != blank %}
          {{ section.settings.image | image_url: width: 1000 | image_tag: loading: 'lazy' }}
        {% else %}
          {{ 'lifestyle-1' | placeholder_svg_tag: 'placeholder-svg' }}
        {% endif %}
        
        <div class="itt-image-overlay"></div>

        {% if section.settings.handwriting_text != blank %}
          <div class="itt-handwriting-wrap">
            <div class="itt-handwriting">{{ section.settings.handwriting_text }}</div>
            <svg class="itt-arrow-svg" viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M10 20 Q 30 70, 80 80" stroke-width="4" stroke-linecap="round" fill="transparent"/>
              <path d="M60 70 L 80 80 L 70 95" stroke-width="4" stroke-linecap="round" fill="transparent"/>
            </svg>
          </div>
        {% endif %}

        {% if section.settings.hosted_video != blank %}
          <div class="itt-play-btn" role="button" aria-label="Play video">
            <svg viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg>
          </div>
        {% endif %}
      </div>

      {% if section.settings.show_trust_bar %}
        <div class="itt-trust-bar">
          <div class="itt-trust-left">
            <div class="itt-avatars">
              {% if section.settings.avatar_1 != blank %}
                {{ section.settings.avatar_1 | image_url: width: 80 | image_tag: class: 'itt-avatar' }}
              {% endif %}
              {% if section.settings.avatar_2 != blank %}
                {{ section.settings.avatar_2 | image_url: width: 80 | image_tag: class: 'itt-avatar' }}
              {% endif %}
              {% if section.settings.avatar_3 != blank %}
                {{ section.settings.avatar_3 | image_url: width: 80 | image_tag: class: 'itt-avatar' }}
              {% endif %}
            </div>
            <div class="itt-trust-text">
              {{ section.settings.trust_text_bold }}
              <span>{{ section.settings.trust_text_sub }}</span>
            </div>
          </div>

          <div class="itt-trust-right">
            <div class="itt-rating-box">{{ section.settings.rating_score }}</div>
            <div class="itt-stars-wrap">
              <div class="itt-stars">
                {%- for i in (1..5) -%}
                  <svg viewBox="0 0 24 24"><path d="M12 .587l3.668 7.568 8.332 1.151-6.064 5.828 1.48 8.279-7.416-3.967-7.417 3.967 1.481-8.279-6.064-5.828 8.332-1.151z"/></svg>
                {%- endfor -%}
              </div>
              <div class="itt-reviews-count">{{ section.settings.review_count_text }}</div>
            </div>
          </div>
        </div>
      {% endif %}
    </div>
  </div>

  {% if section.settings.hosted_video != blank %}
    <div class="itt-video-modal" role="dialog" aria-modal="true" aria-label="Video Player">
      <div class="itt-modal-overlay"></div>
      <div class="itt-modal-content">
        <button class="itt-close-btn" aria-label="Close video">&times;</button>
        {{ section.settings.hosted_video | video_tag: controls: true, autoplay: false, loop: false, width: '100%' }}
      </div>
    </div>
  {% endif %}
</section>

<script>
  (function() {
    function initSection() {
      const section = document.querySelector("#ImageTrustSection-{{ section_id }}");
      if (!section) return;

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('itt-visible');
          }
        });
      }, { threshold: 0.15 });

      section.querySelectorAll('.itt-animate').forEach(el => observer.observe(el));

      const playBtn = section.querySelector('.itt-play-btn');
      const modal = section.querySelector('.itt-video-modal');
      
      if (playBtn && modal) {
        const closeBtn = modal.querySelector('.itt-close-btn');
        const overlay = modal.querySelector('.itt-modal-overlay');
        const videoEl = modal.querySelector('video');

        playBtn.addEventListener('click', () => { modal.classList.add('active'); videoEl?.play(); });
        [closeBtn, overlay].forEach(el => el.addEventListener('click', () => { modal.classList.remove('active'); videoEl?.pause(); }));
      }
    }

    document.addEventListener("DOMContentLoaded", initSection);
    if (Shopify.designMode) {
      document.addEventListener('shopify:section:load', e => { if (e.detail.sectionId === '{{ section_id }}') initSection(); });
    }
  })();
</script>

{% schema %}
{
  "name": "PRO Image & Trust Text",
  "settings": [
    {
      "type": "header",
      "content": "📐 Layout"
    },
    {
      "type": "range", "id": "max_width", "min": 800, "max": 1800, "step": 20, "unit": "px", "label": "Container Width", "default": 1300
    },
    {
      "type": "range", "id": "column_gap", "min": 20, "max": 120, "step": 5, "unit": "px", "label": "Column Gap", "default": 60
    },
    {
      "type": "select", "id": "desktop_content_order", "label": "Desktop Content Position",
      "options": [
        { "value": "2", "label": "Left (Text first)" },
        { "value": "1", "label": "Right (Media first)" }
      ],
      "default": "2"
    },
    {
      "type": "select", "id": "mobile_content_order", "label": "Mobile Content Position",
      "options": [
        { "value": "2", "label": "Top (Text above)" },
        { "value": "1", "label": "Bottom (Media above)" }
      ],
      "default": "2"
    },
    {
      "type": "select", "id": "text_align", "label": "Text Alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ],
      "default": "left"
    },
    {
      "type": "header",
      "content": "📝 Content"
    },
    {
      "type": "text", "id": "subtitle", "label": "Subtitle", "default": "YOUR FUTURE IS OFFSHORE"
    },
    {
      "type": "textarea", "id": "title", "label": "Heading", "default": "We Help You Get Hired in the Norwegian Offshore Industry"
    },
    {
      "type": "richtext", "id": "text", "label": "Paragraph", "default": "<p>Work Offshore is your trusted guide to enter the offshore oil and gas industry in Norway.</p>"
    },
    {
      "type": "text", "id": "btn_text", "label": "Button Text", "default": "LEARN MORE"
    },
    {
      "type": "url", "id": "btn_link", "label": "Button Link"
    },
    {
      "type": "checkbox", "id": "btn_full_width", "label": "Full Width Button", "default": false
    },
    {
      "type": "header",
      "content": "🎨 Media & Handwriting"
    },
    {
      "type": "image_picker", "id": "image", "label": "Cover Image"
    },
    {
      "type": "video", "id": "hosted_video", "label": "Upload Video"
    },
    {
      "type": "color", "id": "overlay_color", "label": "Image Overlay Color", "default": "#000000"
    },
    {
      "type": "range", "id": "overlay_opacity", "min": 0, "max": 80, "step": 5, "unit": "%", "label": "Overlay Opacity", "default": 0
    },
    {
      "type": "textarea", "id": "handwriting_text", "label": "Handwritten Text", "default": "Real people.\nReal jobs."
    },
    {
      "type": "font_picker", "id": "handwriting_font", "label": "Handwriting Font", "default": "assistant_n4"
    },
    {
      "type": "select", "id": "handwriting_weight", "label": "Handwriting Weight",
      "options": [
        { "value": "400", "label": "Regular" },
        { "value": "600", "label": "Semi-Bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "700"
    },
    {
      "type": "color", "id": "handwriting_color", "label": "Text Color", "default": "#FFFFFF"
    },
    {
      "type": "text", "id": "handwriting_shadow", "label": "Text Shadow (CSS)", "default": "0 2px 4px rgba(0,0,0,0.3)", "info": "e.g., 0 2px 4px rgba(0,0,0,0.3)"
    },
    {
      "type": "checkbox", "id": "show_arrow", "label": "Show Arrow", "default": true
    },
    {
      "type": "range", "id": "arrow_size", "min": 20, "max": 100, "step": 2, "unit": "px", "label": "Arrow Size", "default": 40
    },
    {
      "type": "range", "id": "hand_pos_v", "min": 0, "max": 100, "step": 1, "unit": "%", "label": "Vertical Position", "default": 15
    },
    {
      "type": "range", "id": "hand_pos_h", "min": 0, "max": 100, "step": 1, "unit": "%", "label": "Horizontal Position", "default": 6
    },
    {
      "type": "range", "id": "hand_rotation", "min": -45, "max": 45, "step": 1, "unit": "deg", "label": "Rotation", "default": 0
    },
    {
      "type": "header",
      "content": "⭐ Trust Bar"
    },
    {
      "type": "checkbox", "id": "show_trust_bar", "label": "Show Trust Bar", "default": true
    },
    {
      "type": "image_picker", "id": "avatar_1", "label": "Avatar 1"
    },
    {
      "type": "image_picker", "id": "avatar_2", "label": "Avatar 2"
    },
    {
      "type": "image_picker", "id": "avatar_3", "label": "Avatar 3"
    },
    {
      "type": "text", "id": "trust_text_bold", "label": "Main Text", "default": "Join thousands who changed their life"
    },
    {
      "type": "text", "id": "trust_text_sub", "label": "Sub Text", "default": "with an offshore career"
    },
    {
      "type": "text", "id": "rating_score", "label": "Rating Score", "default": "4.9/5"
    },
    {
      "type": "text", "id": "review_count_text", "label": "Reviews", "default": "From 1,000+ reviews"
    },
    {
      "type": "header",
      "content": "🔤 Typography"
    },
    {
      "type": "range", "id": "title_size_desk", "min": 20, "max": 80, "step": 2, "unit": "px", "label": "Heading Size Desktop", "default": 42
    },
    {
      "type": "range", "id": "title_size_mob", "min": 18, "max": 50, "step": 2, "unit": "px", "label": "Heading Size Mobile", "default": 32
    },
    {
      "type": "range", "id": "title_lh", "min": 8, "max": 20, "step": 1, "label": "Heading Line Height (x/10)", "default": 12
    },
    {
      "type": "range", "id": "title_ls", "min": -10, "max": 20, "step": 10, "unit": "em", "label": "Letter Spacing", "default": 0
    },
    {
      "type": "select", "id": "title_transform", "label": "Text Transform",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "uppercase", "label": "Uppercase" }
      ],
      "default": "none"
    },
    {
      "type": "range", "id": "subtitle_size_desk", "min": 10, "max": 24, "step": 1, "unit": "px", "label": "Subtitle Size Desktop", "default": 14
    },
    {
      "type": "range", "id": "subtitle_size_mob", "min": 10, "max": 20, "step": 1, "unit": "px", "label": "Subtitle Size Mobile", "default": 12
    },
    {
      "type": "range", "id": "subtitle_ls", "min": 0, "max": 50, "step": 10, "unit": "em", "label": "Subtitle Letter Spacing", "default": 10
    },
    {
      "type": "select", "id": "subtitle_transform", "label": "Subtitle Transform",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "uppercase", "label": "Uppercase" }
      ],
      "default": "uppercase"
    },
    {
      "type": "range", "id": "text_size_desk", "min": 12, "max": 24, "step": 1, "unit": "px", "label": "Body Size Desktop", "default": 16
    },
    {
      "type": "range", "id": "text_size_mob", "min": 12, "max": 20, "step": 1, "unit": "px", "label": "Body Size Mobile", "default": 15
    },
    {
      "type": "range", "id": "text_lh", "min": 10, "max": 20, "step": 1, "label": "Body Line Height (x/10)", "default": 16
    },
    {
      "type": "range", "id": "handwriting_size_desk", "min": 20, "max": 80, "step": 2, "unit": "px", "label": "Handwriting Size Desktop", "default": 36
    },
    {
      "type": "range", "id": "handwriting_size_mob", "min": 16, "max": 40, "step": 1, "unit": "px", "label": "Handwriting Size Mobile", "default": 24
    },
    {
      "type": "header",
      "content": "🎨 Colors"
    },
    { "type": "color", "id": "bg_color", "label": "Background", "default": "#FAFAFA" },
    { "type": "color", "id": "title_color", "label": "Heading", "default": "#0B192C" },
    { "type": "color", "id": "text_color", "label": "Body", "default": "#555555" },
    { "type": "color", "id": "highlight_color", "label": "Accent", "default": "#C79A3A" },
    { "type": "color", "id": "btn_bg", "label": "Button Background", "default": "#0B192C" },
    { "type": "color", "id": "btn_text_color", "label": "Button Text", "default": "#FFFFFF" },
    { "type": "color", "id": "feature_bg", "label": "Feature Box Background", "default": "#FFFFFF" },
    { "type": "color", "id": "feature_text_color", "label": "Feature Box Text", "default": "#0B192C" },
    { "type": "color", "id": "feature_border", "label": "Feature Border", "default": "#EAEAEA" },
    { "type": "color", "id": "card_bg", "label": "Card Background", "default": "#FFFFFF" },
    {
      "type": "header",
      "content": "📏 Spacing & Radius"
    },
    {
      "type": "range", "id": "padding_top_desktop", "min": 0, "max": 160, "step": 4, "unit": "px", "label": "Top Padding Desktop", "default": 80
    },
    {
      "type": "range", "id": "padding_bottom_desktop", "min": 0, "max": 160, "step": 4, "unit": "px", "label": "Bottom Padding Desktop", "default": 80
    },
    {
      "type": "range", "id": "padding_top_mobile", "min": 0, "max": 100, "step": 4, "unit": "px", "label": "Top Padding Mobile", "default": 48
    },
    {
      "type": "range", "id": "padding_bottom_mobile", "min": 0, "max": 100, "step": 4, "unit": "px", "label": "Bottom Padding Mobile", "default": 48
    },
    {
      "type": "range", "id": "btn_radius", "min": 0, "max": 40, "step": 2, "unit": "px", "label": "Button Radius", "default": 6
    },
    {
      "type": "range", "id": "btn_padding", "min": 8, "max": 24, "step": 2, "unit": "px", "label": "Button Padding Vertical", "default": 16
    },
    {
      "type": "range", "id": "btn_padding_x", "min": 16, "max": 48, "step": 2, "unit": "px", "label": "Button Padding Horizontal", "default": 28
    },
    {
      "type": "range", "id": "feature_radius", "min": 0, "max": 20, "step": 2, "unit": "px", "label": "Feature Radius", "default": 6
    },
    {
      "type": "range", "id": "card_radius", "min": 0, "max": 40, "step": 2, "unit": "px", "label": "Card Radius", "default": 16
    }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "Feature Tag",
      "limit": 6,
      "settings": [
        {
          "type": "text", "id": "title", "label": "Feature Text", "default": "Proven Strategy"
        },
        {
          "type": "select", "id": "icon_type", "label": "Icon Type",
          "options": [
            { "value": "default", "label": "Default Shield Icon" },
            { "value": "image", "label": "Custom Image" },
            { "value": "svg", "label": "Custom SVG Code" }
          ],
          "default": "default"
        },
        {
          "type": "image_picker", "id": "icon_image", "label": "Icon Image (PNG/SVG)"
        },
        {
          "type": "textarea", "id": "svg_code", "label": "SVG Code", "info": "Paste raw SVG code"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "PRO Image & Trust Text",
      "blocks": [
        { "type": "feature", "settings": { "title": "Proven Strategy" } },
        { "type": "feature", "settings": { "title": "Industry Knowledge" } },
        { "type": "feature", "settings": { "title": "Professional Support" } },
        { "type": "feature", "settings": { "title": "Real Results" } }
      ]
    }
  ]
}
{% endschema %}